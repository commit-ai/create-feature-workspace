# Tasks to Do Or to Consider

1. Add the capability to create a workspace without worktrees (that uses/references each repository in the workspace with its current local branch - synchronized)
2. Add support for adding/removing repositories/folders from a workspace
3. Should this be a global bash/PS command or a Claude Code plugin?
